import 'package:flutter/material.dart';

double displayHeight(BuildContext context) => MediaQuery.sizeOf(context).height;

double displayWidth(BuildContext context) => MediaQuery.sizeOf(context).width;
